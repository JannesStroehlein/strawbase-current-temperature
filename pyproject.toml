[project]
name = "strawbase-current-temperature"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "babel>=2.18.0",
    "flask>=3.1.3",
    "homeassistant-api>=6.0.1",
]
